const double t4 = m * m;
const double t6 = 0.1e1 / t4 / m;
const double t10 = E * E;
const double Dreg = -0.3e1 / 0.16e2 * E * (E - m) * (E + m) * t6 + (0.3e1 / 0.8e1 * Q * (t10 + t4) * t6 * q - 0.3e1 / 0.4e1 * E / m * M) / r0;
